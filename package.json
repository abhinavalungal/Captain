{
  "name": "kris",
  "version": "1.0.0",
  "private": true,
  "description": "K.R.1.S \u2014 a Krishna-inspired vessel-data assistant with a deterministic core (never fabricates a figure), an instant local fast lane, and a streamed LLM layer for conversation and app help.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node test/run.js && node test/instant_test.js && node test/persona_test.js && node test/agent_test.js && node test/companion_test.js && node test/reasoning_test.js && node test/stream_test.js && node test/widget.js && node test/widget_memory.js && node test/profile_test.js && node test/server.js && node test/cors_test.js && node test/auth_test.js",
    "test:offline": "KRIS_TEST_URL= npm test",
    "bench": "node test/latency_bench.js",
    "discover": "node scripts/discover.js",
    "sync": "node scripts/sync.js"
  },
  "dependencies": {
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
